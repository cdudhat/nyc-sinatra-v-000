<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Figure</title>
  </head>
  <body>
    <h2>Create a New Figure</h2>
    <form class="" action="/figures" method="post">
      <p>Figure Name: <input type="text" name="figure[name]" id="figure_name" value=""></p>
      <p>Landmark: (Choose an existing)</p>
      <% Landmark.all.each do |landmark| %>
        <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>"> <%= landmark.name %>
      <% end %>
      <p>Or create a new Landmark:</p>
      <input type="text" name="landmark[name]" id="new_landmark" value="">
      <br></br>
      <p>Title: (Choose an existing)</p>
      <% Title.all.each do |title| %>
        <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>"> <%= title.name %>
      <% end %>
      <p>Or create a new Title:</p>
      <input type="text" name="title[name]" id="new_title" value="">
      <br></br>
      <input type="submit" value="Create New Figure">
    </form>
  </body>
</html>
